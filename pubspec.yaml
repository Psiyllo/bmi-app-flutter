name: learningapp
description: "A new Flutter project."
publish_to: 'none' # Remove esta linha se você quiser publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter:
    sdk: flutter
  sqflite: ^2.2.7
  sqflite_common_ffi: ^2.2.7
  sqflite_common_ffi_web: ^0.4.3+1
  path: ^1.8.3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/images/
  uses-material-design: true
